// GENERATED — do not edit by hand. Run `npm run stamp` after changing extraction
// code, the same way a rule change needs the goldens re-frozen.
//
// A digest of the extraction sources, frozen here so the line can stamp a record
// without reading the source tree at runtime. `ka-factory`'s `computeExtractionDigest`
// produces it and a test fails when the two disagree, so the value cannot go stale
// unnoticed — which is the whole point: the previous stamp could not move on its
// own, and so it never did.
export const EXTRACTION_DIGEST = "0401d40b231a";
